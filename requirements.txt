# This file was autogenerated by uv via the following command:
#    uv pip compile pyproject.toml -o requirements.txt
aiofiles==25.1.0

# === Основной фреймворк Telegram-бота ===
aiogram~=3.22.0

# HTTP-клиент (используется aiogram + кастомная сессия)
aiohttp~=3.13.2

# Поддержка прокси (нужен, если используешь PROXY)
aiohttp-socks~=0.9.1

# === Планировщик задач (напоминания, фоновые задания) ===
apscheduler~=3.11.2

# === Работа с переменными окружения (.env) ===
python-dotenv~=1.2.1

# === Интеграция с Google Sheets ===
gspread~=6.2.1
oauthlib~=3.2.2

# === Валидация данных (используется aiogram, можно фиксировать для стабильности) ===
pydantic~=2.12.5